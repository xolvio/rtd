var Router, RouteController;

(function () {
    "use strict";

    var emptyFunction = function () {
    };

    Router = {
        map: emptyFunction,
        configure: emptyFunction
    };

    RouteController = {
        extend: emptyFunction
    };

})();
